---
title: 'Retrieve Fund Wallet Request'
api: 'GET /wallet/fund/requests/{requestId}'
authMethod: 'bearer'
---

## Path parameters

<ParamField path="requestId" type="string" required>
  Path parameter `requestId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="data" type="object">
  Request body field `data`.
</ParamField>

<ParamField body="data.id" type="string">
  Request body field `data.id`.
</ParamField>

<ParamField body="data.depositorName" type="string">
  Request body field `data.depositorName`.
</ParamField>

<ParamField body="data.toWallet" type="string">
  Request body field `data.toWallet`.
</ParamField>

<ParamField body="data.amount" type="string">
  Request body field `data.amount`.
</ParamField>

<ParamField body="data.description" type="string">
  Request body field `data.description`.
</ParamField>

<ParamField body="data.status" type="string">
  Request body field `data.status`.
</ParamField>

<ParamField body="data.transferType" type="string">
  Request body field `data.transferType`.
</ParamField>

<ParamField body="data.depositInformation" type="object">
  Request body field `data.depositInformation`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryName" type="string">
  Request body field `data.depositInformation.beneficiaryName`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryRoutingNumber" type="string">
  Request body field `data.depositInformation.beneficiaryRoutingNumber`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryAccountNumber" type="string">
  Request body field `data.depositInformation.beneficiaryAccountNumber`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryAccountType" type="string">
  Request body field `data.depositInformation.beneficiaryAccountType`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryBankAddress" type="string">
  Request body field `data.depositInformation.beneficiaryBankAddress`.
</ParamField>

<ParamField body="data.depositInformation.beneficiaryBankSwift" type="string">
  Request body field `data.depositInformation.beneficiaryBankSwift`.
</ParamField>

<ParamField body="data.depositInformation.specialInstruction" type="string">
  Request body field `data.depositInformation.specialInstruction`.
</ParamField>

<ParamField body="data.updatedAt" type="string">
  Request body field `data.updatedAt`.
</ParamField>

<ParamField body="data.createdAt" type="string">
  Request body field `data.createdAt`.
</ParamField>

## Request

Endpoint: `GET /wallet/fund/requests/{requestId}`

### Example

```bash
curl --location "$BASE_URL/wallet/fund/requests/{requestId}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "data": {
    "id": "4750621f-b429-4766-a733-9aa243d6abe9",
    "depositorName": "JOHN DOE",
    "toWallet": "86f24597-a2a4-489d-b18d-c2bfb9b8b452",
    "amount": "25000.00",
    "description": "Wallet funding",
    "status": "PENDING",
    "transferType": "domestic-wire",
    "depositInformation": {
      "beneficiaryName": "ABC LLC",
      "beneficiaryRoutingNumber": "121140399",
      "beneficiaryAccountNumber": "3304236682",
      "beneficiaryAccountType": "Business Checking",
      "beneficiaryBankAddress": "3003 Tasman Drive, Santa Clara, CA 95054, USA",
      "beneficiaryBankSwift": "SVBKUS6S",
      "specialInstruction": "ZM OF33 OCW ABC LLC"
    },
    "updatedAt": "2024-10-29T06:13:34.000000Z",
    "createdAt": "2024-10-29T06:13:34.000000Z"
  }
}
```
