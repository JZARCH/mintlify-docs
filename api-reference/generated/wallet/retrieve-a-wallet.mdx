---
title: 'Retrieve a wallet'
api: 'GET /wallet/{uuid}'
authMethod: 'bearer'
---

## Path parameters

<ParamField path="uuid" type="string" required>
  Path parameter `uuid`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="data" type="object">
  Request body field `data`.
</ParamField>

<ParamField body="data.id" type="string">
  Request body field `data.id`.
</ParamField>

<ParamField body="data.availableBalance" type="string">
  Request body field `data.availableBalance`.
</ParamField>

<ParamField body="data.currentBalance" type="string">
  Request body field `data.currentBalance`.
</ParamField>

<ParamField body="data.bankAccount_id" type="string">
  Request body field `data.bankAccount_id`.
</ParamField>

<ParamField body="data.walletName" type="string">
  Request body field `data.walletName`.
</ParamField>

<ParamField body="data.walletType" type="number">
  Request body field `data.walletType`.
</ParamField>

<ParamField body="data.bankAccountName" type="string">
  Request body field `data.bankAccountName`.
</ParamField>

<ParamField body="data.bankAccountNickName" type="string">
  Request body field `data.bankAccountNickName`.
</ParamField>

<ParamField body="data.bankAccountNumber" type="string">
  Request body field `data.bankAccountNumber`.
</ParamField>

<ParamField body="data.bankAccountNatureType" type="string">
  Request body field `data.bankAccountNatureType`.
</ParamField>

## Request

Endpoint: `GET /wallet/{uuid}`

### Example

```bash
curl --location "$BASE_URL/wallet/{uuid}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "data": {
    "id": "838f298c-24f0-4638-8af4-479f39e4a228",
    "availableBalance": "18804.92",
    "currentBalance": "18894.92",
    "bankAccount_id": null,
    "walletName": "Master Wallet",
    "walletType": 2,
    "bankAccountName": null,
    "bankAccountNickName": null,
    "bankAccountNumber": "X",
    "bankAccountNatureType": null
  }
}
```
