---
title: 'Mark as Posted'
api: 'POST /checks/{checkId}/mark-as-cleared'
authMethod: 'bearer'
---

## Path parameters

<ParamField path="checkId" type="string" required>
  Path parameter `checkId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="message" type="string">
  Request body field `message`.
</ParamField>

## Request

Endpoint: `POST /checks/{checkId}/mark-as-cleared`

### Example

```bash
curl --location "$BASE_URL/checks/{checkId}/mark-as-cleared" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "message": "Successfully marked as cleared"
}
```
