---
title: 'Retrieve Wallet Gift Card'
api: 'GET /card-program/gift-card/{giftCardId}'
authMethod: 'bearer'
---

Retrieves the details of a specific gift card using its unique ID.

## Path parameters

<ParamField path="giftCardId" type="string" required>
  Path parameter `giftCardId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="data" type="object">
  Request body field `data`.
</ParamField>

<ParamField body="data.id" type="string">
  Request body field `data.id`.
</ParamField>

<ParamField body="data.walletId" type="string">
  Request body field `data.walletId`.
</ParamField>

<ParamField body="data.status" type="string">
  Request body field `data.status`.
</ParamField>

<ParamField body="data.card" type="object">
  Request body field `data.card`.
</ParamField>

<ParamField body="data.card.id" type="string">
  Request body field `data.card.id`.
</ParamField>

<ParamField body="data.card.cardProgramIssuingRecordId" type="string">
  Request body field `data.card.cardProgramIssuingRecordId`.
</ParamField>

<ParamField body="data.card.cardType" type="string">
  Request body field `data.card.cardType`.
</ParamField>

<ParamField body="data.card.label" type="string">
  Request body field `data.card.label`.
</ParamField>

<ParamField body="data.card.nameOnCard" type="string">
  Request body field `data.card.nameOnCard`.
</ParamField>

<ParamField body="data.card.limitAmount" type="string">
  Request body field `data.card.limitAmount`.
</ParamField>

<ParamField body="data.card.limitInterval" type="string">
  Request body field `data.card.limitInterval`.
</ParamField>

<ParamField body="data.card.last4" type="string">
  Request body field `data.card.last4`.
</ParamField>

<ParamField body="data.card.cardStatus" type="string">
  Request body field `data.card.cardStatus`.
</ParamField>

<ParamField body="data.card.expiryMonth" type="string">
  Request body field `data.card.expiryMonth`.
</ParamField>

<ParamField body="data.card.expiryYear" type="string">
  Request body field `data.card.expiryYear`.
</ParamField>

<ParamField body="data.card.holder" type="object">
  Request body field `data.card.holder`.
</ParamField>

<ParamField body="data.card.holder.payeeId" type="string">
  Request body field `data.card.holder.payeeId`.
</ParamField>

<ParamField body="data.card.holder.name" type="string">
  Request body field `data.card.holder.name`.
</ParamField>

<ParamField body="data.card.holder.email" type="string">
  Request body field `data.card.holder.email`.
</ParamField>

<ParamField body="data.card.holder.address1" type="string">
  Request body field `data.card.holder.address1`.
</ParamField>

<ParamField body="data.card.holder.address2" type="string">
  Request body field `data.card.holder.address2`.
</ParamField>

<ParamField body="data.card.holder.city" type="string">
  Request body field `data.card.holder.city`.
</ParamField>

<ParamField body="data.card.holder.state" type="string">
  Request body field `data.card.holder.state`.
</ParamField>

<ParamField body="data.card.holder.country" type="string">
  Request body field `data.card.holder.country`.
</ParamField>

<ParamField body="data.card.holder.zip" type="string">
  Request body field `data.card.holder.zip`.
</ParamField>

<ParamField body="data.card.cardIssueType" type="string">
  Request body field `data.card.cardIssueType`.
</ParamField>

<ParamField body="data.availableBalance" type="string">
  Request body field `data.availableBalance`.
</ParamField>

<ParamField body="data.updatedAt" type="string">
  Request body field `data.updatedAt`.
</ParamField>

<ParamField body="data.createdAt" type="string">
  Request body field `data.createdAt`.
</ParamField>

## Request

Endpoint: `GET /card-program/gift-card/{giftCardId}`

### Example

```bash
curl --location "$BASE_URL/card-program/gift-card/{giftCardId}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "data": {
    "id": "ad51a361-cb91-4d69-a2d6-66a62867f232",
    "walletId": "dd55fcc4-84a6-400b-b033-c7b2520c7963",
    "status": "COMPLETED",
    "card": {
      "id": "d009d218-c5f1-4fac-8d08-facb47766582",
      "cardProgramIssuingRecordId": "ad51a361-cb91-4d69-a2d6-66a62867f232",
      "cardType": "virtual",
      "label": "Ethan Harris",
      "nameOnCard": "Ethan Harris",
      "limitAmount": "75.00",
      "limitInterval": "allTime",
      "last4": "6204",
      "cardStatus": "pending_activation",
      "expiryMonth": "11",
      "expiryYear": "2031",
      "holder": {
        "payeeId": "dNDOGnBn2oG0oMX",
        "name": "Ethan Harris",
        "email": "ethan.harris@email.com",
        "address1": "1111 Franklin Avenue",
        "address2": null,
        "city": "Valley Stream",
        "state": "NY",
        "country": "US",
        "zip": "11580"
      },
      "cardIssueType": "RECHARGEABLE_GIFT_CARD"
    },
    "availableBalance": "75.00",
    "updatedAt": "2025-03-20T04:55:28.000000Z",
    "createdAt": "2025-03-20T04:55:23.000000Z"
  }
}
```
