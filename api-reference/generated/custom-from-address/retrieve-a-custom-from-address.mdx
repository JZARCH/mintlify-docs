---
title: 'Retrieve a custom from address'
api: 'GET /customFromAddresses/{customFromAddressId}'
authMethod: 'bearer'
---

Details of custom from address.

Path parameters

 
  customFromAddressId
  Required    
         The id of the Custom from address to be retrieved.
  



## Path parameters

<ParamField path="customFromAddressId" type="string" required>
  Path parameter `customFromAddressId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="data" type="object">
  Request body field `data`.
</ParamField>

<ParamField body="data.id" type="string">
  Request body field `data.id`.
</ParamField>

<ParamField body="data.name" type="string">
  Request body field `data.name`.
</ParamField>

<ParamField body="data.companyName" type="string">
  Request body field `data.companyName`.
</ParamField>

<ParamField body="data.addressLine1" type="string">
  Request body field `data.addressLine1`.
</ParamField>

<ParamField body="data.addressLine2" type="string">
  Request body field `data.addressLine2`.
</ParamField>

<ParamField body="data.city" type="string">
  Request body field `data.city`.
</ParamField>

<ParamField body="data.state" type="string">
  Request body field `data.state`.
</ParamField>

<ParamField body="data.zip" type="string">
  Request body field `data.zip`.
</ParamField>

<ParamField body="data.phone" type="string">
  Request body field `data.phone`.
</ParamField>

<ParamField body="data.email" type="string">
  Request body field `data.email`.
</ParamField>

<ParamField body="data.note" type="string">
  Request body field `data.note`.
</ParamField>

## Request

Endpoint: `GET /customFromAddresses/{customFromAddressId}`

### Example

```bash
curl --location "$BASE_URL/customFromAddresses/{customFromAddressId}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "data": {
    "id": "gpNlkQ4rqjwnOV0",
    "name": "Stella R Pelton",
    "companyName": null,
    "addressLine1": "2915  Frederick Street",
    "addressLine2": "9466 Del Monte Ave.Lindenhurst, NY 11757",
    "city": "SAINT PETERS",
    "state": "MO",
    "zip": "63376",
    "phone": "900244400",
    "email": "email@test.com",
    "note": null
  }
}
```
