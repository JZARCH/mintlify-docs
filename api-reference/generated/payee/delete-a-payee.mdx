---
title: 'Delete a Payee'
api: 'DELETE /payees/{payeeId}'
authMethod: 'bearer'
---

Permanently deletes a payee. 

Path parameters

 
  payeeId
  Required    
         The id of the payee to delete
  



## Path parameters

<ParamField path="payeeId" type="string" required>
  Path parameter `payeeId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="message" type="string">
  Request body field `message`.
</ParamField>

## Request

Endpoint: `DELETE /payees/{payeeId}`

### Example

```bash
curl --location "$BASE_URL/payees/{payeeId}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "message": "Succesfully deleted"
}
```
