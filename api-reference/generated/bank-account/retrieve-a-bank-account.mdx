---
title: 'Retrieve a bank account'
api: 'GET /bankAccounts/{bankAccountId}'
authMethod: 'bearer'
---

Details of an existing bank account.

**Path parameters**

bankAccountIdRequiredThe id of the Bank account to be retrieved.

## Path parameters

<ParamField path="bankAccountId" type="string" required>
  Path parameter `bankAccountId`.
</ParamField>

## Body parameters

<ParamField body="success" type="boolean">
  Request body field `success`.
</ParamField>

<ParamField body="data" type="object">
  Request body field `data`.
</ParamField>

<ParamField body="data.id" type="string">
  Request body field `data.id`.
</ParamField>

<ParamField body="data.bankId" type="string">
  Request body field `data.bankId`.
</ParamField>

<ParamField body="data.name" type="string">
  Request body field `data.name`.
</ParamField>

<ParamField body="data.nickName" type="string">
  Request body field `data.nickName`.
</ParamField>

<ParamField body="data.accountNumber" type="string">
  Request body field `data.accountNumber`.
</ParamField>

<ParamField body="data.addressLine1" type="string">
  Request body field `data.addressLine1`.
</ParamField>

<ParamField body="data.addressLine2" type="string">
  Request body field `data.addressLine2`.
</ParamField>

<ParamField body="data.city" type="string">
  Request body field `data.city`.
</ParamField>

<ParamField body="data.state" type="string">
  Request body field `data.state`.
</ParamField>

<ParamField body="data.zip" type="string">
  Request body field `data.zip`.
</ParamField>

<ParamField body="data.phone" type="string">
  Request body field `data.phone`.
</ParamField>

<ParamField body="data.isVerified" type="boolean">
  Request body field `data.isVerified`.
</ParamField>

<ParamField body="data.isAchEnabled" type="boolean">
  Request body field `data.isAchEnabled`.
</ParamField>

<ParamField body="data.createdDate" type="string">
  Request body field `data.createdDate`.
</ParamField>

<ParamField body="data.bankAccountId" type="string">
  Request body field `data.bankAccountId`.
</ParamField>

<ParamField body="data.RoutingNumber" type="string">
  Request body field `data.RoutingNumber`.
</ParamField>

<ParamField body="data.webUrl" type="string">
  Request body field `data.webUrl`.
</ParamField>

## Request

Endpoint: `GET /bankAccounts/{bankAccountId}`

### Example

```bash
curl --location "$BASE_URL/bankAccounts/{bankAccountId}" \
  --header "Authorization: Bearer $AUTH_TOKEN" \
  --header "Accept: application/json"
```

## Response example

```json
{
  "success": true,
  "data": {
    "id": "kY2VjXWXz8eNEmJ",
    "bankId": "xZqXGK2xEz4RzVv",
    "name": "Demo Bank Account",
    "nickName": "Demo Bank Account 1",
    "accountNumber": "111111111111",
    "addressLine1": "1111 Franklin Avenue",
    "addressLine2": null,
    "city": "Garden City",
    "state": "NY",
    "zip": "11530-1617",
    "phone": null,
    "isVerified": false,
    "isAchEnabled": false,
    "createdDate": "02-27-2024",
    "bankAccountId": "xQkY2VjXWXz8eNEmJ78",
    "RoutingNumber": "011000015",
    "webUrl": "{{BASE_URL}}/manage/bank-accounts/index?id=kY2VjXWXz8eNEmJ"
  }
}
```
